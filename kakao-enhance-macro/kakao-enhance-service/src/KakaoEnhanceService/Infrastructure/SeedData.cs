namespace KakaoEnhanceService.Infrastructure
{
    internal static class SeedData
    {
        public static void Seed(InMemoryStore store)
        {
            // Production-oriented default: start with an empty store.
            // Users should register their first player through the UI or chatbot.
        }
    }
}
